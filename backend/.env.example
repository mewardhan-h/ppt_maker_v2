# NVIDIA API Key for content generation and formatting (Stage 1 & 2)
NVIDIA_API_KEY=your_nvidia_api_key_here

# Note: GEMINI_API_KEY is only needed if you use utils/llm_formatter.py separately
# The main app (app.py) only uses NVIDIA API
